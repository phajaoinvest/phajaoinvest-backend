version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: phajaoinvest_db
    env_file:
      - .env
    ports:
      - "5431:5432"
    volumes:
      - ../phajaoinvest_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: phajaoinvest_redis
    ports:
      - "6371:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: phajaoinvest_backend
    env_file:
      - .env
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    restart: unless-stopped

volumes:
  redis_data:
